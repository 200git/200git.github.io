<?xml version="1.0" encoding="utf-8"?>
<search> 
  
    
    <entry>
      <title><![CDATA[利用Hexo+GitHub+Cloudflare搭建静态网站]]></title>
      <url>/2023/07/14/%E5%88%A9%E7%94%A8Hexo-GitHub-Cloudflare%E6%90%AD%E5%BB%BA%E9%9D%99%E6%80%81%E7%BD%91%E7%AB%99/</url>
      <content type="html"><![CDATA[<blockquote class="blockquote-center">以后的路上，不必强迫而顺然。</blockquote>

<p>搭建一个属于自己的博客是我一直想做的，以前尝试直接用GitHub Pages搭建，但是单纯访问readme文档都卡的要死，最近发现Hexo搭建的很方便，Cloudflare也能加速访问GitHub Pages，自己在中间踩了很多坑，故写在这里防止其他人走不必要的弯路…</p>
<h2 id="准备工作"><a href="#准备工作" class="headerlink" title="准备工作"></a>准备工作</h2><h3 id="Hexo"><a href="#Hexo" class="headerlink" title="Hexo"></a>Hexo</h3><p>Hexo是一款基于Node.js的静态博客框架，可以方便地生成静态网页挂载在GitHub和Heroku上，是搭建博客的首选框架，这里我们就选用Hexo来搭建。<br>需要准备的软件</p>
<figure class="highlight plaintext"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br></pre></td><td class="code"><pre><span class="line">Git: https://git-scm.com/</span><br><span class="line">Node.js: http://nodejs.cn/</span><br></pre></td></tr></table></figure>
<p>准备在本地搭建博客的文件夹中右键选择”Git bash here”，当然在地址栏中键入cmd也是可以的。<br>进入窗口后输入命令：</p>
<figure class="highlight cmd"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br></pre></td><td class="code"><pre><span class="line">$ npm intstall hexo-cli -g</span><br><span class="line">$ hexo init blog</span><br><span class="line">$ <span class="built_in">cd</span> blog</span><br><span class="line">$ npm install</span><br><span class="line">$ hexo server</span><br></pre></td></tr></table></figure>
<p>完成以上工作后访问<code>http://localhost:4000</code>就能看到已经搭建的本地博客，其中主题为默认主题<code>landscape</code>。<br>你可以通过查询Hexo命令来进行文章发布的操作，也可以通过修改<code>_config.yml</code>来进行设置主题等操作，这些在官网都有相关文档。<code>_config.yml</code></p>
<h3 id="CloudFlare"><a href="#CloudFlare" class="headerlink" title="CloudFlare"></a>CloudFlare</h3><figure class="highlight plaintext"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line">CloudFlare官网:https://cloudflare.com/</span><br></pre></td></tr></table></figure>
<p>Cloudflare能提供安全服务和加速代理，所以这里选用Cloudflare加速Github Pages。</p>
<h4 id="获取Cloudflare账号"><a href="#获取Cloudflare账号" class="headerlink" title="获取Cloudflare账号"></a>获取Cloudflare账号</h4><p>注册即可。</p>
<p>在Pages界面里绑定Github账号，设置对应库即可，免费版每天boundle访问次数有10w，作为静态网站（如个人博客）再合适不过。</p>
<h3 id="Gitub-Pages"><a href="#Gitub-Pages" class="headerlink" title="Gitub Pages"></a>Gitub Pages</h3><p>你肯定需要一个平台来承载你的静态网站内容，这里建议用GitHub而不是Gitee，虽然Gitee是国内服务器访问速度快，但是安全性无法保证，当然，你也可以考虑用<code>Gitee+Hexo</code>来构建你的个人网站。</p>
<p><strong>优点：免备案，搭建简单。缺点：访问慢，所以要加速，会繁琐一些。</strong></p>
<h4 id="新建GitHub仓库"><a href="#新建GitHub仓库" class="headerlink" title="新建GitHub仓库"></a>新建GitHub仓库</h4><p>我们在完成Hexo的本地部署后，要连接Git以方便我们进行快速便捷地修改上传博客到Gitub上。我们要先在GitHub上新建一个库，库的名字为<strong>用户名</strong>.github.io（如200git.github.io）。</p>
<h4 id="将库和本地文件夹绑定"><a href="#将库和本地文件夹绑定" class="headerlink" title="将库和本地文件夹绑定"></a>将库和本地文件夹绑定</h4><p>生成SSH-keys，首先检查ssh完整性，在本地任意文件夹位置处Git bash，随后输入ssh以检查是否安装ssh，然后在命令框中输入：</p>
<figure class="highlight cmd"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line">$ ssh-keygen -t rsa -C &quot;注册Github时使用的邮件地址&quot;</span><br></pre></td></tr></table></figure>
<p>然后<strong>敲四次回车。</strong>（有可能是三次，<del>我忘了。</del>）</p>
<p>随后输入：</p>
<figure class="highlight cmd"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line">$ cat ~/.ssh/id_rsa.pub</span><br></pre></td></tr></table></figure>
<p>并将输出内容复制到Github设置中的SSH key框中，title无特殊要求可随意起名。注意<code>id_rsa</code>私钥不能泄露，而<code>id_rsa.pub</code>是公钥所以可以提供或分享。</p>
<p>最后测试ssh是否绑定成功：</p>
<figure class="highlight cmd"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line">$ ssh -T git@github.com</span><br></pre></td></tr></table></figure>
<p>到这一步我们就完成了本地库和GitHub远程库绑定啦~</p>
<p>随后确定用户名和邮箱：</p>
<figure class="highlight cmd"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br></pre></td><td class="code"><pre><span class="line">$ git config --global user.name &quot;你的用户名&quot;</span><br><span class="line">$ git config --global user.email &quot;你的邮箱地址&quot;</span><br></pre></td></tr></table></figure>
<p>然后在本地博客的文件夹下修改<code>_config.yml</code>，补充配置文件最后的<code>deploy</code>部分，具体内容如下：**(留意冒号后边的空格！！没有空格会报错！！)**</p>
<figure class="highlight plaintext"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br></pre></td><td class="code"><pre><span class="line">deploy:</span><br><span class="line">  type: git</span><br><span class="line">  repository: 你刚才创建的Github地址</span><br><span class="line">  branch: main</span><br></pre></td></tr></table></figure>
<p>修改配置文件保存后，在博客文件夹下Git bash，安装<code>hexo-deployer-git</code>：</p>
<figure class="highlight cmd"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line">$ npm intstall hexo-deployer-git --save</span><br></pre></td></tr></table></figure>
<p>然后生成网页：</p>
<figure class="highlight cmd"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line">$ hexo g</span><br></pre></td></tr></table></figure>
<p>部署网页到GitHub远程库：</p>
<figure class="highlight cmd"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line">$ hexo d</span><br></pre></td></tr></table></figure>
<p>然后我们就可以通过GitHub或者Cloudflare提供的多级地址来访问我们的静态网页啦~<br><strong>（如果你遇到输入密码或者输入令牌的提示语句，你可以通过<del>查百度</del>的方法来解决，比我讲的细致多了）</strong></p>
<h3 id="域名（可选）"><a href="#域名（可选）" class="headerlink" title="域名（可选）"></a>域名（可选）</h3><p><strong>这可不是推销啊，域名的选择是自己决定的！！！</strong></p>
<p>使用GitHub或者Cloudflare所给的多级地址访问可能记起来过于繁琐（我的笨脑子应该记不下来），所以有一个便捷访问又具有个性的域名是一个不错的选择。<br>申请域名的地方很多很多，可以选择阿里云、腾讯云之类的，新用户还有优惠！（比如我这个域名zeratemi.fun一年也就9块钱，首年1.29元，而且fun也意为快乐~ ^ ^</p>
<p>这里以腾讯云为例，腾讯云会分配免费的DNS来解析域名，但如果我们要使用Coudflare来代理加速我们的网页，我们就要用Cloudflare提供的名称服务器来作为DNS解析，而具体的设置流程，都能在对应官网找到解答办法，这里不多赘述。<br><strong>修改名称服务器的审核可能需要一段时间，这和平台本身有关，但我估计最慢也不会超过1小时，当Cloudflare处检查名称服务器通过时，会发送邮件来提醒你名称服务器已经修改成功。并且再三强调，一定一定一定要等Cloudflare检查通过冒绿色对勾了再检查域名，否则再怎么看也是卡到host处。</strong></p>
<h3 id="博客文章"><a href="#博客文章" class="headerlink" title="博客文章"></a>博客文章</h3><p>Hexo的文章使用的语言是轻量化标记语言Markdown，这种语言很好上手的，表现起来也很方便，教程网上都有，看一遍学会满足基本需求的部分就行。</p>
<p>放一首喜欢的歌，我觉得很好听。</p>
<iframe frameborder="no" border="0" marginwidth="0" marginheight="0" width=530 height=86 src="//music.163.com/outchain/player?type=2&id=1847030625&auto=0&height=66"></iframe>]]></content>
      
        
    </entry>
    
  
  
</search>
